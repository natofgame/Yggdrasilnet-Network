namespace Yggdrasilnet.Network.Packet.Snapshot;

public enum NetworkedComponentType : byte {
    Velocity = 1,
    Action = 2,
    Health = 3,
    Projectile = 4,
    Collision = 5,
    Direction = 6,
}